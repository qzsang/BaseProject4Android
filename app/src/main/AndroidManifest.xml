<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qzsang.baseproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- sdcard读权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- sdcard写权限 -->
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".face.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".face.simple.SimpleActivity" />
        <activity android:name=".face.simple.SimpleFragmentActivity" />
        <activity android:name=".face.simple.SimpleWebActivity" />
        <activity android:name=".face.simple.SimpleListActivity" />
        <activity android:name=".face.simple.SimpleImageViewActivity" />
        <activity android:name=".face.simple.SimpleNetActivity" />
        <activity android:name=".face.simple.SimpleDialogActivity"></activity>
    </application>

</manifest>